
⸻

1. 背景与要解决的问题

你与各负责人对话后会获得对话转写文本（txt / pdf / word），目前存在以下痛点：
	•	信息散落在长文本中，复盘成本高
	•	容易遗漏“需要你拍板/需要你介入”的关键事项
	•	负责人画像缺乏结构化沉淀，历史信息难维护
	•	行动项无法快速进入执行系统（自研 ToDo）

本模块目标是将“对话文本 → COO视角结构化条目 → 你审核确认 → 入库可长期维护”，并将行动项一键推送到自研 ToDo。

⸻

2. 产品目标与成功标准

2.1 产品目标（V1）
	1.	快速结构化：上传一次文本 → 自动按固定 6 维度提炼条目
	2.	可控入库：必须经过你审核确认后才写入数据库
	3.	持续可维护：负责人管理界面可长期编辑条目（增删改/移动维度）
	4.	可执行：行动项结构化并支持一键推送到自研 ToDo

2.2 成功标准（上线后评估建议）
	•	审核成本下降：从读全文变为“看条目 + 少量编辑”
	•	拍板/介入事项漏掉的频率下降（你主观感受 + 回溯验证）
	•	行动项进入 ToDo 的转化率提升（推送使用率上升）

⸻

3. 范围与非目标

3.1 V1 范围（必须做）
	•	从负责人详情页上传（强绑定负责人，避免误绑）
	•	支持上传并解析：txt / docx / pdf（文字版优先）
	•	LLM 按 6 维度生成条目草稿（拍板事项分两档）
	•	同一次上传内部语义去重合并（输出“不重复清单”）
	•	审核页：Existing（历史）+ Draft（本次）对照；支持条目增删改/移动维度
	•	确认入库：增量追加（旧条目保留）
	•	管理页：按维度维护历史条目
	•	行动项：结构化 + 一键推送到自研 ToDo（实现方式由工程适配）

3.2 非目标（V1 不做）
	•	不提供维度模板编辑（维度固定 6 个，不改名不增删）
	•	不做跨历史自动去重/自动合并
	•	不解析 ETA 到具体日期（只存原文）
	•	不做自动识别负责人/会议时间（不关注会议时间）
	•	不做多人权限系统（单人用，但保留扩展点）

⸻

4. 用户画像与典型场景

4.1 用户画像
	•	唯一用户：COO（你）
	•	核心诉求：更快掌握负责人状态、捕捉决策点、推动行动进入执行系统

4.2 核心场景（Top 5）
	1.	与负责人 A 对话后，在 A 的详情页上传文本 → 得到结构化草稿
	2.	在审核页删改几条、把某条从“困扰”移到“风险” → 确认入库
	3.	快速查看“本次必须拍板/需要介入”两类事项
	4.	把若干行动项一键推送到自研 ToDo → 进入执行流
	5.	回到负责人管理页清理/重写条目，保持档案可用

⸻

5. 信息架构与维度口径

5.1 固定 6 维度（不可编辑）
	1.	负责人的关注点
	•	对方反复提及/最在意的主题、指标、依赖、约束等
	2.	负责人的目标
	•	明确或隐含目标（短中期均可）
	3.	负责人困扰
	•	卡点、阻碍、抱怨、无法推进的原因
	4.	本次需要我拍板的事情（同一维度两档）
	•	必须拍板（must_decide）：决策/批准/定优先级/资源分配/方案选择
	•	需要介入（need_intervene）：协调/推动/对齐/关键知会（不一定是决策）
	5.	负责人感觉到的风险
	•	可能导致结果/进度/质量/团队/外部合作变差的风险
	6.	负责人的行动项和 ETA
	•	结构化：行动项（action） + ETA原文（eta_text，可空）

⸻

6. 端到端流程

6.1 上传入口（防止负责人出错）
	•	入口只存在于：
	•	负责人详情页按钮【上传对话】
	•	或负责人卡片内【上传对话】
	•	从该入口上传的文件 自动绑定该负责人
	•	不提供“选择负责人”的下拉

6.2 处理链路
	1.	上传文件
	2.	解析文本（抽取纯文本）
	3.	调用 LLM 生成结构化 Draft
	4.	同次语义去重合并（同维度内）
	5.	打开审核页（Existing vs Draft）

6.3 审核与入库
	•	审核页展示：
	•	Existing：已入库历史条目
	•	Draft：本次提取条目（可编辑、可移动）
	•	点击【确认入库】：
	•	Draft 以增量方式写入 confirmed items
	•	Draft 中的证据句子（evidence）不落库、入库后清理

⸻

7. 功能需求（详细）

7.1 文件上传与解析

支持文件类型
	•	txt
	•	docx
	•	pdf（文字版优先）

解析要求
	•	提取纯文本用于 LLM
	•	记录元信息：
	•	文件类型、字符数、页数（如可得）、上传时间戳

错误处理
	•	空文件/解析失败：提示失败原因，可重新上传
	•	超长文本：按分段策略处理（见 7.3 性能）

⸻

7.2 LLM 提取（COO视角）

输出必须满足
	•	输出包含 6 维度（无条目维度可输出空数组/或隐藏展示）
	•	条目短、清晰、可执行、避免冗长背景描述
	•	拍板维度必须带类型：
	•	must_decide 或 need_intervene
	•	行动项维度必须结构化：
	•	action 必填
	•	eta_text 可空

⸻

7.3 同次语义去重合并（同一次上传内部）

目标
	•	同一次上传的输出中，同维度内语义相近条目应合并为一条“代表条目”，确保列表“不重复”。

约束
	•	仅在同一维度内合并
	•	阈值保守：宁可多留一条，不要把不同事项揉成一条
	•	允许保留 merged_from 元信息用于调试（UI 默认不展示）

推荐实现（工程可二选一或组合）
	•	方案 A：LLM 在生成时就遵守“同维度不输出语义重复” + 后处理兜底
	•	方案 B：Embedding 相似度聚类 + 用 LLM 合并改写为 1 条代表条目

⸻

7.4 审核页（核心页面）

页面结构
	•	顶部：负责人信息 + 文件信息 + 处理状态
	•	左侧：Existing（历史条目，按维度折叠）
	•	右侧：Draft（本次条目，按维度折叠）

Draft 条目操作
	•	编辑（inline）
	•	删除
	•	新增条目
	•	移动到其他维度
	•	若属于拍板维度：切换 must_decide / need_intervene

证据展示（仅审核阶段）
	•	Draft 条目支持“展开显示证据一句话”
	•	入库后证据句子不保存到条目

审核页按钮
	•	【确认入库】
	•	【放弃本次草稿】（保留原始文件，不入库条目）

⸻

7.5 入库与历史管理

入库策略
	•	增量追加：本次确认的条目追加写入；旧条目不覆盖
	•	跨历史不去重：允许长期堆叠（用管理能力对冲）

管理页（长期维护）
	•	负责人详情页提供入口【洞察管理】
	•	视图：按 6 维度展示所有历史条目
	•	操作：编辑 / 删除 / 新增 / 移动维度
	•	必须能力（对冲条目膨胀）：
	•	搜索
	•	折叠/展开
	•	默认按“最近新增/最近更新”排序

⸻

7.6 行动项 → 自研 ToDo 一键推送

范围
	•	仅针对「行动项和 ETA」维度条目

推送内容建议
	•	title: [负责人名] 行动项：{action}
	•	description（可选）：
	•	ETA: {eta_text}
	•	来源：负责人系统条目链接/负责人页链接

工程实现要求（不限定接法）
	•	推送成功：返回并记录 todo_task_id（用于防重复/回跳）
	•	推送失败：展示错误原因 + 可重试

推送触发方式（自动/逐条/批量）当前未最终确认；V1 默认建议做“逐条手动推送”，降低误创建风险。

⸻

8. 数据记录与质量回路

8.1 建议记录字段
	•	Artifact（一次上传）
	•	owner_id, file_type, char_count, created_at
	•	Extraction Run
	•	model_name, prompt_version, status, latency_ms
	•	Draft Item
	•	dimension, 内容字段、是否被编辑/删除/移动、证据句子（仅 Draft）
	•	Confirmed Item
	•	dimension, 内容字段、created_from_artifact_id, created_at, updated_at
	•	ToDo Link
	•	confirmed_item_id, todo_task_id, 推送状态

8.2 建议埋点指标
	•	每次上传：各维度条目数
	•	Draft 编辑率/删除率/新增率/移动率
	•	拍板事项两档分布
	•	ToDo 推送率与失败率

⸻

9. 安全与数据保留
	•	原始文件：私有存储（单人也应避免误泄露）
	•	Draft 证据句子：仅审核可见；确认入库后清理
	•	建议保留审计日志（你后续追溯“我改过什么”非常关键）

⸻

10. 性能与限制
	•	支持长文本：采用分段（chunk）→ 分段抽取 → 汇总合并策略
	•	超长文本：提示“已分段处理”，可能存在少量信息损失；建议拆分上传
	•	失败可重试：解析失败/模型失败/ToDo 推送失败均可重试，不影响历史数据

⸻

11. 验收标准（测试用例级）
	1.	从负责人页上传文件，系统自动绑定负责人，无选择负责人的入口
	2.	生成 Draft：结构正确、包含 6 维度、拍板事项有两档
	3.	同次语义相近条目会被合并，最终列表无明显重复
	4.	审核页支持：编辑/删除/新增/移动维度/拍板两档切换
	5.	确认入库后：历史条目仍在，新条目追加出现
	6.	入库后条目中不保存证据句子（接口也读不到）
	7.	管理页支持对历史条目增删改/移动维度，并支持搜索
	8.	行动项可推送 ToDo：成功记录 todo_task_id；失败有提示可重试

⸻

12. 风险与对策（关键）

风险 1：跨历史不去重导致条目膨胀
	•	对策（V1 必须）：管理页搜索/折叠/排序
	•	对策（V2 可选）：引入“疑似重复标记”或“归档/状态”

风险 2：同次语义合并误伤造成信息损失
	•	对策：阈值保守；仅同维度合并；保留 merged_from 便于调参

风险 3：入库不保存证据句子导致长期信任下降
	•	对策：至少保留来源 artifact_id 与原文件可回看；保留上传时间戳

风险 4：ToDo 推送误创建噪音任务
	•	对策：V1 默认“逐条手动推送”或“勾选批量推送”；记录 todo_task_id 防重复