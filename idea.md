我想创建一个工具的集合服务，使用同一套前段和后端包含数据库的架构
首页包含所有功能的快速入口，所有的相关的参数配置都统一在统一的配置页内完成。主页需要有基本的登陆保证安全


我想开发的的第一工具是 to-do list
我通过粘贴文字，上传文件/图片，通过大模型的解读，能够准确的提取出一条/多条 任务，负责人，完成时间，dod等信息，在我确认/修改后插入数据库
通过页签我也可以管理现在所有的to do 列表，并能够 筛选/排序。 同时每一条任务我都能够修改，删除，或者一键完成，完成后的任务就不需要显示在任务列表中，
任务列表也要用颜色标识出今天要完成的任务和7天内要完成的任务
我同时需要维护一个负责人的数据库列表，在添加/修改任务的时候，负责人是不存在数据库中，要提醒是否增加新的负责人，确认后插入数据库。
我同时还希望有一个后台程序，每天早上8点通过飞书发送信息到指定的群里，通知今天要完成的任务 和 本周需要完成的任务